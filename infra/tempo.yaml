server:
  http_listen_port: 8000

distributor:
  receivers:
    otlp:
      protocols:
        http:
          endpoint: "tempo:4318"

storage:
  trace:
    backend: local
    wal:
      path: /tmp/tempo/wal
    local:
      path: /tmp/tempo/blocks
